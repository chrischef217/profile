package com.unityglobal.unity_drive_access;

// Source body is generated in CI from the preserved v2.3 baseline and patch_visual_tree.py.
// This placeholder is never compiled directly because the workflow restores the validated baseline before applying the v2.5 compact map patch.
final class MainActivitySourceMarker {
    static final String VERSION = "v2.5-compact-map";
}
